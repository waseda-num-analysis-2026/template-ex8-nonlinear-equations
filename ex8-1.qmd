---
title: "Exercise 8.1 — Multi-Dimensional Newton's Method"
subtitle: "Numerical Analysis 2026 — Lecture 8"
format:
  html:
    toc: true
    toc-depth: 3
    code-tools: true
    highlight-style: github
execute:
  echo: true
  warning: false
jupyter: python3
---

## Problem

Implement **Newton's method** for a **2D or higher-dimensional** system of
nonlinear equations

$$
F(\mathbf{x}) = \mathbf{0}, \qquad F : \mathbb{R}^n \to \mathbb{R}^n,
$$

where the Jacobian $J_F(\mathbf{x})$ is used at each step:

$$
\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - J_F(\mathbf{x}^{(k)})^{-1} F(\mathbf{x}^{(k)}).
$$

**Tasks:**

- Implement the multi-dimensional Newton's method
- Solve a total of **3 examples**
  - **2 examples**: choose yourself (do **not** use the circle and parabola
    example from the handout)
  - **1 example**: ask AI to choose (state which AI you used and what
    prompt you gave)
- For each example, report the number of iterations and the final residual
  $\|F(\mathbf{x}^{(k)})\|$
- Briefly discuss your observations (convergence speed, sensitivity to
  initial guess, etc.)

AI is permitted. AI is **recommended** for plotting and for selecting the
AI-chosen example.

---

## 1. Implementation

Complete the Newton iteration. Solve the linear system $J_F \Delta\mathbf{x} = -F$
at each step using `np.linalg.solve` instead of computing $J_F^{-1}$ explicitly.

```{python}
import numpy as np
import matplotlib.pyplot as plt

def newton_nd(F, J, x0, max_iter=50, tol=1e-10):
    """
    Multi-dimensional Newton's method.

    Parameters
    ----------
    F : callable  F(x) -> 1-D array of length n
    J : callable  J(x) -> 2-D array of shape (n, n)   (Jacobian of F)
    x0 : array-like  initial guess (length n)
    max_iter : int
    tol : float  stopping criterion  ||F(x)|| < tol

    Returns
    -------
    x_history : list of 1-D arrays  (length = number of steps taken + 1)
    res_history : list of floats    ||F(x^(k))||
    converged : bool
    """
    x = np.array(x0, dtype=float)
    x_history = [x.copy()]
    res_history = [np.linalg.norm(F(x))]

    for k in range(max_iter):
        Fx = F(x)
        Jx = J(x)
        # TODO: solve J(x) * delta = -F(x) for delta, then update x
        delta = None  # replace with np.linalg.solve(...)
        x = x + delta
        res = np.linalg.norm(F(x))
        x_history.append(x.copy())
        res_history.append(res)
        if res < tol:
            return x_history, res_history, True

    return x_history, res_history, False
```

---

## 2. Example 1 (your choice)

State the system $F(\mathbf{x}) = \mathbf{0}$ you chose and why, then solve it.
Do **not** use the circle-and-parabola example from the handout.

> **System:** *(describe here)*

```{python}
def F1(x):
    # TODO: return np.array([..., ...])
    pass

def J1(x):
    # TODO: return np.array([[..., ...], [..., ...]])
    pass

x0_1 = None  # TODO: choose an initial guess

# hist1, res1, ok1 = newton_nd(F1, J1, x0_1)
# print(f"Converged: {ok1},  iterations: {len(hist1)-1},  final residual: {res1[-1]:.2e}")
```

---

## 3. Example 2 (your choice)

State the system and solve it.  Do **not** use the circle-and-parabola example.

> **System:** *(describe here)*

```{python}
def F2(x):
    # TODO
    pass

def J2(x):
    # TODO
    pass

x0_2 = None  # TODO

# hist2, res2, ok2 = newton_nd(F2, J2, x0_2)
# print(f"Converged: {ok2},  iterations: {len(hist2)-1},  final residual: {res2[-1]:.2e}")
```

---

## 4. Example 3 (AI-chosen)

Ask an AI to propose a 2D (or higher) nonlinear system and solve it.
State:
1. Which AI you used and what prompt you gave.
2. What system was proposed.
3. Whether you verified the result (e.g. checked that $F(\mathbf{x}^*) \approx \mathbf{0}$).

> **AI used:** *(e.g. ChatGPT-4o)*
>
> **Prompt given:** *(write here)*
>
> **Proposed system:** *(write here)*

```{python}
def F3(x):
    # TODO: AI-chosen system
    pass

def J3(x):
    # TODO
    pass

x0_3 = None  # TODO

# hist3, res3, ok3 = newton_nd(F3, J3, x0_3)
# print(f"Converged: {ok3},  iterations: {len(hist3)-1},  final residual: {res3[-1]:.2e}")
```

---

## 5. Summary table

Fill in the table after running your experiments.
Copy values from `res_history` and `len(x_history)-1`; do not write them by hand.

| Example | System (brief) | $n$ | $\mathbf{x}^{(0)}$ | Converged? | Iterations | Final $\|F\|$ |
|---------|---------------|-----|---------------------|------------|------------|---------------|
| 1 | | | | | | |
| 2 | | | | | | |
| 3 (AI) | | | | | | |

: **Summary table (fill after running your code).**

---

## 6. Convergence plots

Plot $\|F(\mathbf{x}^{(k)})\|$ vs. iteration $k$ for each example (log scale
on the $y$-axis is recommended to visualise quadratic convergence).

```{python}
# TODO: your plots
# Example structure:
# fig, ax = plt.subplots()
# for label, res in [("Example 1", res1), ("Example 2", res2), ("Example 3 (AI)", res3)]:
#     ax.semilogy(res, marker='o', label=label)
# ax.set_xlabel("Iteration k")
# ax.set_ylabel(r"$\|F(\mathbf{x}^{(k)})\|$")
# ax.legend()
# ax.set_title("Newton's method — residual history")
# plt.tight_layout()
# plt.show()
```

---

## 7. Discussion

Briefly answer the following:

1. Did all three examples converge? If not, what went wrong?
2. Do you observe quadratic convergence (residual roughly squaring each
   iteration)? Show evidence from your data.
3. How sensitive was convergence to the choice of initial guess?

> **Your answer:**
>
> *(write here)*

---

## AI Usage

Check one:

- [ ] I did not use AI.
- [ ] I used AI for debugging / plotting / explanation only.
- [ ] I used AI more substantially, and I verified the result myself.

If you used AI, briefly describe how:

> *(write here)*
